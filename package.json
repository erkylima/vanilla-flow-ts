{
  "name": "rspack-react-starter",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "NODE_ENV=development DEBUG=TRUE rspack serve",
    "build": "NODE_ENV=production rspack build",
    "debug": "node --inspect --debug-brk"
  },
  "devDependencies": {
    "@rspack/cli": "0.6.3",
    "@rspack/core": "0.6.3",
    "typescript": "^5.4.5",
    "typescript-plugin-css-modules": "^5.1.0"
  }
}
