{
  "name": "rspack-react-starter",
  "private": true,
  "version": "1.0.0",
  "author": "Érky Lima",
  "license": "MIT",
  "description": "This is a Node Based UI Library with WebComponent support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "NODE_ENV=development rspack serve",
    "build-rspack": "NODE_ENV=production rspack build",
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "yarn run clean && yarn run build"
  },
  "devDependencies": {
    "@rspack/cli": "0.6.3",
    "@rspack/core": "0.6.3",
    "tslib": "^2.6.3",
    "typescript-plugin-css-modules": "^5.1.0"
  },
  "dependencies": {
    "vanilla-flow": "^1.0.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/erkylima/vanilla-flow-ts.git"
  }
}
